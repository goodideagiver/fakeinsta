{
  "name": "fakeinsta",
  "version": "1.0.0",
  "description": "fake instagram OOP app",
  "browserslist": "> 2.0%, last 2 versions, not dead",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf dist && parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url .",
    "deploy": "gh-pages -d dist"
  },
  "author": "Purple Black",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.4.0",
    "parcel": "^2.4.0",
    "sharp": "^0.29.3"
  },
  "dependencies": {
    "@swc/helpers": "^0.3.8",
    "gh-pages": "^3.2.3",
    "regenerator-runtime": "^0.13.9"
  }
}
